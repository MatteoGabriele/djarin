export default function Home() {
  return (
    <div className="bg-zinc-50 flex items-center justify-center h-screen">
      <p className="text-2xl text-zinc-500">{{project.name}}</p>
    </div>
  );
}
